export interface TrailPoint {
  x: number;
  y: number;
  r: number;
  alpha: number;
  seed: number;
}

export interface NavItem {
  id: string;
  label: string;
  href: string;
  leftDesktop: string;
  scaleX: number;
}
